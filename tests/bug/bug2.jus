//Token dont convert comma (.) currect but just save the char code. 
//The int converter dont convert . Probely. It can convert it to , (the danish comma).
use "error";

if("89.12" != 89.12+""){
  error("The parser dont convert comma correct: "+89.12);
}